def train_model(dataset_path="data/", epochs=10, batch_size=8):
    """Placeholder for model training."""
    print(f"Training model on {dataset_path} for {epochs} epochs...")
    print("Training complete!")

if __name__ == "__main__":
    train_model()
